<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="party.qwer.irisgui">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".IrisGUIApplication"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/irislitelogo"
        android:label="IrisGUI"
        android:roundIcon="@mipmap/irislitelogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IrisGUI"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IrisGUI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- W^X 실측 하네스(임시, 연구 후 제거) — adb 로 am start 하여 logcat 에서 확인. -->
        <activity
            android:name=".WxProbeActivity"
            android:exported="true"
            android:theme="@style/Theme.IrisGUI" />
        <activity
            android:name=".WxProbe2Activity"
            android:exported="true"
            android:theme="@style/Theme.IrisGUI" />
        <activity
            android:name=".WxProbe3Activity"
            android:exported="true"
            android:theme="@style/Theme.IrisGUI" />

        <service
            android:name=".service.IrisService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Notification monitoring bot daemon" />
        </service>

        <service
            android:name=".service.IrisNotificationService"
            android:label="IrisGUI Notification Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>

        <receiver
            android:name=".service.WatchdogReceiver"
            android:enabled="true"
            android:exported="true">
            <!-- ISSUE-33: HEARTBEAT 는 manifest filter 에서 제거 — AlarmManager PendingIntent 의
                 명시적 컴포넌트 브로드케이트로 계속 전달되며, 외부 앱의 action 남용으로 인한
                 시작 경로 churn 을 줄인다. BOOT/MY_PACKAGE_REPLACED 는 시스템 보호 방송만 온다. -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
